const reverse = (str) => {
     let newStr = newStr + str[str.length];
     
//   if (newStr.length === str.length) {
//       return newStr;
//   }  
   return newStr;
 };
 
 console.log(reverse('str'));